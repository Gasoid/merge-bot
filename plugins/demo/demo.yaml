name: Hello plugin
command: "!hello"
runtime: "wasm"

wasm_config:
  exported_function: "hello"
  path: "https://github.com/Gasoid/merge-bot/plugins/demo/plugin.wasm"
  env_vars:
  - demo_name
  allowed_hosts:
  - "api.host.com"
